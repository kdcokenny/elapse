# ===========================================
# Elapse Configuration
# ===========================================
# Local dev:  cp .env.example .env
# Docker:     docker compose up
# Platforms:  Set these vars in your platform's UI
#
# See deploy/ for platform-specific guides
# ===========================================

# ===========================================
# Required
# ===========================================
GOOGLE_GENERATIVE_AI_API_KEY=
DISCORD_WEBHOOK_URL=

# ===========================================
# GitHub App
# ===========================================
# Leave empty to use the setup wizard
# For Docker/platforms: use base64-encoded private key
#   cat your-app.pem | base64
#
# GITHUB_APP_ID=
# GITHUB_APP_PRIVATE_KEY=
# GITHUB_WEBHOOK_SECRET=

# ===========================================
# Report Configuration
# ===========================================

# Report cadence: "weekly" (default), "daily", or "both"
REPORT_CADENCE=weekly

# Weekly schedule (default: Friday 4pm)
# Alternative: "0 9 * * 1" for Monday 9am
WEEKLY_SCHEDULE=0 16 * * 5

# Daily schedule (only used if REPORT_CADENCE includes daily)
DAILY_SCHEDULE=0 9 * * 1-5

# Optional: channel-specific webhooks (override DISCORD_WEBHOOK_URL)
# Lookup order - Daily: DISCORD_WEBHOOK_URL_DAILY → DISCORD_WEBHOOK_URL
# Lookup order - Weekly: DISCORD_WEBHOOK_URL_WEEKLY → DISCORD_WEBHOOK_URL
# DISCORD_WEBHOOK_URL_DAILY=
# DISCORD_WEBHOOK_URL_WEEKLY=

# ===========================================
# Optional
# ===========================================
PROJECT_CONTEXT=A software project
TEAM_TIMEZONE=America/New_York
LLM_MODEL_NAME=gemini-flash-latest
LOG_LEVEL=info
PORT=3000

# ===========================================
# Infrastructure
# ===========================================
# Local:    redis://localhost:6379
# Docker:   Automatically set in compose files
# Railway:  Use Railway's Redis add-on (auto-injected)
# Dokploy:  Automatically set in compose files
#
# REDIS_URL=redis://localhost:6379
